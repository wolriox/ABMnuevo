<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/proveedores/editar/{{proveedor.idProveedor}}" method="POST">
                    <div class="form-group">
                        <input type="text" value="{{proveedor.idProveedor}}" class="form-control" name="idProveedor" placeholder="Código de Proveedor" disabled>
                    </div>
                    <div class="form-group">
                        <input type="text" value="{{proveedor.nombreProveedor}}" class="form-control" name="nombreProveedor" placeholder="Nombre/Empresa" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="text" value="{{proveedor.pDireccion}}" class="form-control" name="pDireccion" placeholder="Dirección">
                    </div>
                    <div class="form-group">
                        <input type="text" value="{{proveedor.pTelefono}}" class="form-control" name="pTelefono" placeholder="Teléfono">
                    </div>
                    <div class="form-group">
                        <input type="text" value="{{proveedor.pContacto}}" class="form-control" name="pContacto" placeholder="Nombre de Contacto">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">
                            Guardar
                        </button>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>